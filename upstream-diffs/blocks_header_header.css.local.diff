diff --git a/blocks/header/header.css b/blocks/header/header.css
index 10ca4bab..a286fa97 100644
--- a/blocks/header/header.css
+++ b/blocks/header/header.css
@@ -1,7 +1,7 @@
 :root {
   --desktop-item-panel-max-width: 300px;
   --desktop-item-panel-min-width: 200px;
-  --desktop-large-panel-width: 900px;
+  --desktop-large-panel-width: 1024px;
   --desktop-panel-min-height: 200px;
   --desktop-panel-max-height: 600px;
 }
@@ -12,7 +12,19 @@ header .nav-wrapper {
   width: 100%;
   z-index: 3;
   position: fixed;
-  border-bottom: 1px solid var(--color-neutral-300);
+  border-bottom: 1px solid var(--color-neutral-400);
+  display: flex;
+  flex-direction: column-reverse;
+}
+
+.nav-wrapper::after {
+  content: '';
+  display: block;
+  width: 100%;
+  height: 1rem;
+  position: absolute;
+  top: 100%;
+  background: linear-gradient(to bottom, rgb(0 0 0 / 15%) 0%,rgb(0 0 0 / 0%) 100%);
 }
 
 header nav {
@@ -25,6 +37,7 @@ header nav {
   gap: 0 16px;
   margin: auto;
   max-width: 1248px;
+  width: 100%;
   height: var(--nav-height);
   padding: 0 24px;
   font-family: var(--type-base-font-family);
@@ -134,6 +147,10 @@ header nav[aria-expanded='true'] .nav-hamburger-icon::after {
   transform: rotate(-45deg);
 }
 
+header .nav-wrapper.active .nav-notice {
+  display: none;
+}
+
 @media (min-width: 900px) {
   header nav .nav-hamburger {
     display: none;
@@ -142,9 +159,6 @@ header nav[aria-expanded='true'] .nav-hamburger-icon::after {
 }
 
 /* brand */
-header nav .nav-brand {
-  min-width: 128px;
-}
 
 header nav .nav-brand a {
   grid-area: brand;
@@ -162,10 +176,14 @@ li.nav-drop img {
 }
 
 header nav .nav-brand img {
-  width: 128px;
+  width: 48px;
   height: auto;
 }
 
+header nav .nav-brand img:hover {
+  opacity: 0.65;
+}
+
 /* sections */
 header nav .nav-sections {
   grid-area: sections;
@@ -190,6 +208,7 @@ header nav .nav-sections ul {
 header nav .nav-sections ul > li,
 header nav .nav-sections ul > li > a {
   font: var(--type-body-2-strong-font);
+  font-size: 1.8rem;
   letter-spacing: var(--type-body-2-strong-letter-spacing);
 }
 
@@ -210,6 +229,10 @@ header nav .nav-sections ul > li:empty {
 }
 
 @media (min-width: 900px) {
+  header nav {
+    position: relative;
+  }
+
   header nav .nav-sections {
     display: block;
     visibility: visible;
@@ -256,15 +279,16 @@ header nav .nav-sections ul > li:empty {
   }
 
   header nav .nav-sections .default-content-wrapper > ul > li[aria-expanded='true'] .submenu-wrapper {
-    background-color: var(--color-neutral-50);
+    background-color: var(--color-neutral-200);
     box-sizing: border-box;
     left: 0;
     max-height: var(--desktop-panel-max-height);
     min-height: var(--desktop-panel-min-height);
     padding: var(--spacing-xbig);
     position: absolute;
-    top: var(--spacing-large);
+    top: 64px;
     width: 100%;
+    z-index: 1;
   }
 
   header nav .nav-sections .default-content-wrapper > ul > li .submenu-header {
@@ -356,7 +380,7 @@ header nav .nav-tools button.nav-cart-button[data-count]::after {
   position: absolute;
   top: 0;
   right: -2px;
-  background-color: var(--color-brand-700);
+  background-color: var(--color-brand-500);
   color: var(--color-neutral-100);
   border-radius: 50%;
   width: 1.75em;
@@ -368,7 +392,7 @@ header nav .nav-tools button.nav-cart-button[data-count]::after {
 }
 
 header nav .nav-tools button.nav-wishlist-button {
-  background-image: url('../../icons/heart.svg');
+  background-image: url('../../icons/heart-normal.svg');
   background-repeat: no-repeat;
   background-position: center;
   position: relative;
@@ -770,3 +794,11 @@ li
   max-width: 15rem;
   max-height: 15rem;
 }
+
+.minimal-header-footer header .nav-wrapper .nav-hamburger,
+.minimal-header-footer header .nav-wrapper .nav-sections,
+.minimal-header-footer header .nav-wrapper .wishlist-wrapper,
+.minimal-header-footer header .nav-wrapper .search-wrapper,
+.minimal-header-footer header .nav-wrapper .dropdown-wrapper {
+  display: none;
+}
