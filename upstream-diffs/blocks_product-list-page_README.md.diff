diff --git a/blocks/product-list-page/README.md b/blocks/product-list-page/README.md
index e19888f6..e69de29b 100644
--- a/blocks/product-list-page/README.md
+++ b/blocks/product-list-page/README.md
@@ -1,66 +0,0 @@
-# Product List Page Block
-
-## Overview
-
-The Product List Page block provides comprehensive product listing functionality for both category pages and search results using @dropins/storefront-product-discovery containers. It handles product search, filtering, sorting, pagination, and quick actions (add to cart and wishlist) with dynamic URL parameter management and state synchronization.
-
-## Integration
-
-### Block Configuration
-
-The block reads configuration via `readBlockConfig()`:
-
-- `urlpath` - Category URL path for category pages (when present, filters products by category; when absent, treats as search page)
-
-### URL Parameters
-
-- `q` - Search query phrase for search pages
-- `page` - Current page number for pagination (1-based)
-- `sort` - Sort criteria in format `attribute_direction` (e.g., `price_ASC`, comma-separated for multiple)
-- `filter` - Filter criteria in pipe-separated format with attribute:value pairs (e.g., `color:red|price:10-50`)
-
-### Filter Format
-
-Filters use pipe (`|`) separators with attribute:value pairs:
-- **Single value**: `category:Gear`
-- **Multiple values**: `color:red,blue,green` (comma-separated)
-- **Range values**: `price:10-50` (hyphen-separated for from-to)
-- **Combined**: `color:red,blue|price:10-50|category:Gear`
-
-### Events
-
-#### Event Listeners
-
-- `events.on('search/result', callback, { eager: true })` - Listens for search results before block render to update result info and filter counts
-- `events.on('search/result', callback, { eager: false })` - Listens for search results after block render to update URL parameters
-
-<!-- #### Event Emitters
-
-No events are emitted by this block. -->
-
-## Behavior Patterns
-
-### Page Type Detection
-
-- **Category Page**: When `urlpath` is configured, operates as category page with category filter applied
-- **Search Page**: When no `urlpath` is configured, operates as search page using query parameter
-- **Default Visibility**: All searches filter by visibility: 'Search' or 'Catalog, Search'
-- **URL Synchronization**: All search parameters are synced to URL for shareable state
-
-### User Interaction Flows
-
-1. **Initialization**: Block renders search results, facets, sort options, and pagination based on URL parameters or configuration
-2. **Filtering**: Users can toggle facets sidebar and apply multiple filters with visual count badges
-3. **Sorting**: Users can change sort order (position, price, name, etc.) with immediate results update
-4. **Pagination**: Users can navigate pages with smooth scroll to top behavior
-5. **Quick Add to Cart**: Simple products can be added directly; complex products link to product detail page
-6. **Wishlist Management**: Users can toggle wishlist status for any product from the listing
-7. **Product Navigation**: Product images and details link to full product detail pages
-
-### Error Handling
-
-- **Search Errors**: If search API fails, logs error to console and displays empty state
-- **Empty Results**: Block adds `product-list-page--empty` class when no results found
-- **Invalid Parameters**: Gracefully handles missing or malformed URL parameters with defaults
-- **Complex Products**: Automatically renders "View Details" button instead of direct add to cart
-- **Image Fallback**: Uses AEM Assets image rendering with fallback behavior for missing images
