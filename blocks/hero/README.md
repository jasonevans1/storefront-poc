# Hero Auto Block

## Overview

This is an auto block. It has been updated from the original settings.
- It no longer auto builds if a picture is above an h1 inside of another block
- It considers section data for styling

## Features

- Section metadata style 'dark' changes text color and sets picture to translucent

## Usage

Auto builds if a picture is directly above an h1.